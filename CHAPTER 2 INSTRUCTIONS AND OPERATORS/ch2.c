#include <stdio.h>

int main(){
    int i=10;
    int j=i;
    int a=20,b=20,c=10;
    

    printf("the value of i is %d which is same as j %d\n",i,j);
    printf("the value of a is %d which is same as b %d\n",a,b);
    printf("the value of i is %d which is same as c %d\n",i,c);
    
    
    return 0;
}
